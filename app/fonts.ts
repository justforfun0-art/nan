import { Inter, Sora } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const sora = Sora({
  subsets: ["latin"],
  weight: ["600", "700", "800"],
  display: "swap",
});
